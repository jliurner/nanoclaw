---
id: B31
category: lifecycle
setup: { toggle: on, channel: card, migrated: true }
steps:
  - "(operator re-runs /add-adoption-companion on a group where Receipts is ON.)"
expect:
  - "toggle: line stays ON. (re-install never resets to OFF)"
  - "block: still exactly one adoption:receipts marker pair"
---
Re-install preserves the user’s ON state. HARD-GUARANTEE.
